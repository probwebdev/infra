http:
  routers:
    homeassistant:
      rule: "Host(`hub.home.arpa`)"
      entryPoints:
        - websecure
      service: homeassistant
      tls: {}

  services:
    homeassistant:
      loadBalancer:
        servers:
          - url: "http://ha.home.arpa:8123"
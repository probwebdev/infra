http:
  routers:
    homeassistant:
      rule: "Host(`hub.home.arpa`)"
      entryPoints:
        - web
      service: homeassistant

  services:
    homeassistant:
      loadBalancer:
        servers:
          - url: "http://ha.home.arpa:8123"
http:
  routers:
    cockpit:
      rule: "Host(`homelab.home.arpa`)"
      entryPoints:
        - websecure
      service: cockpit

  services:
    cockpit:
      loadBalancer:
        servers:
          - url: "https://homelab.home.arpa:9090"